@model Project.Models.BulletInfo

@{
    ViewData["Title"] = "Create intern";
}
<h1>Create</h1>

<h4>Description internation</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="createIntern" asp-route-descId="@ViewData["descId"]">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Slot" class="control-label"></label>
                <textarea asp-for="Slot" class="form-control" style="resize: auto;"></textarea>
                <span asp-validation-for="Slot" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-controller="admin" asp-action="news-internal" asp-route-descId="@ViewData["descId"]">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}